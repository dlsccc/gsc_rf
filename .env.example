# API base URL for axios client
VITE_API_BASE_URL=/api

# Keep false to use built-in mock model data.
# Switch to true when backend model APIs are ready.
VITE_ENABLE_MODEL_API=false

# Optional project code used when querying business models
VITE_PROJECT_CODE=

# Enable axios request/response logs in browser console
VITE_HTTP_DEBUG=true

# Legacy pipeline publish endpoint (not used by ruleRelease flow)
VITE_PIPELINE_PUBLISH_ENDPOINT=/pipeline/publish
